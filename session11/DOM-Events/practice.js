//Practice Problem 1: Changing Text
// Create an HTML page with a <p> tag that has an id of "demo". Write JavaScript to change the text of this <p> tag to "Hello, world!" when a button is clicked.
// Hints:
// Use document.getElementById() to select the element.
// Use the textContent property to change the text.

//Practice Problem 2: Adding Elements
//Create an HTML page with an empty <ul> element and a button. Each time the button is clicked, add a new <li> element to the <ul> that says "New Item" followed by a number (e.g., New Item 1, New Item 2).
//HINT: Use document.createElement() to create new elements.
// HINT: Use appendChild() to add elements to the parent node.

// Practice Problem 3: Toggling Classes
// Create an HTML page with a <div> that has a default styling. Add a button that, when clicked, toggles a class on the <div> that changes its color.
//HINT: Use classList.toggle() to add or remove the class when the button is clicked.

// Practice Problem 4: Event Listener for Multiple Items
// Create an HTML page with several <button> elements. Write JavaScript that adds an event listener to each button, so when clicked, it displays an alert with the button's id.
// Hints:
// Use a loop to iterate through all buttons using document.querySelectorAll().
// Attach event listeners inside the loop.

// Practice Problem 5: Removing Elements
// Create an HTML page with several <span> elements inside a <div>, each containing the text "Delete me" and a unique id. Add functionality so that clicking on a <span> removes it from the DOM.
//Hints:
// Use parentNode.removeChild() inside an event listener attached to each <span>.
